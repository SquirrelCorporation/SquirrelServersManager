export enum DeviceStatus {
  REGISTERING = 0,
  ONLINE = 1,
  OFFLINE = 2,
  UNMANAGED = 3,
}

export enum ContainerStatus {
  RUNNING = 'running',
  PAUSED = 'paused',
  UNREACHABLE = 'unreachable'
}

export enum RemoteConnectionMethod {
  SSH = 'ssh',
  HTTP = 'http',
}

export enum ConnectionMethod {
  USER_PWD = 'userPwd',
  TOKENS = 'tokens',
}

export enum ContainerType {
  LXC = 'lxc',
  QEMU = 'qemu'
}

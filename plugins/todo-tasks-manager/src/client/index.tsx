import TodoPluginComponent from "./TodoPluginComponent";

// This file can be used as the entry point if needed,
// but typically components are exposed directly via MF config.

// Export the component for Module Federation exposure
export default TodoPluginComponent;

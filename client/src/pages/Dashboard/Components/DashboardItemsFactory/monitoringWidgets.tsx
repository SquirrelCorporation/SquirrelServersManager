/**
 * Monitoring Widget Definitions
 * Custom monitoring widgets with configurable settings
 */

import React from 'react';
import { DashboardItem } from '../../Core/DashboardWidget.types';

export const monitoringWidgets: DashboardItem[] = [
  // Monitoring widgets can be added here in the future
  // These would be configurable monitoring widgets as opposed to the preset ones
];